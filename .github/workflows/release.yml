name: Release

on:
  release:
    types:
      - published

jobs:
  publish-codex-plugin:
    runs-on: ubuntu-24.04
    permissions:
      contents: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install Bun
        uses: tanaabased/actions/setup-bun@v1.0.0-beta.1
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Check canon
        run: |
          bun run lint
          bun run codex:validate
      - name: Prepare release
        uses: tanaabased/actions/prepare-release@v1.0.0-beta.1
        with:
          commands: |
            version-injector .codex-plugin/plugin.json --style json --version "${{ github.event.release.tag_name }}"
            bun run format:write
            bun run codex:validate
      - name: Publish Codex plugin
        uses: tanaabased/actions/publish-codex-plugin@v1.0.0-beta.1
        with:
          archive-name: tanaab-${{ github.event.release.tag_name }}.tar.gz
          dependency-policy: include-production
          github-token: ${{ github.token }}
          release-tag: ${{ github.event.release.tag_name }}

  publish-repo:
    runs-on: ubuntu-24.04
    permissions:
      contents: read
    steps:
      - name: Checkout code
        uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - name: Install Bun
        uses: tanaabased/actions/setup-bun@v1.0.0-beta.1
      - name: Install dependencies
        run: bun install --frozen-lockfile --ignore-scripts
      - name: Check canon
        run: |
          bun run lint
          bun run codex:validate
      - name: Publish repository
        uses: tanaabased/actions/publish-repo@v1.0.0-beta.1
        with:
          commands: |
            version-injector .codex-plugin/plugin.json --style json --version "${{ github.event.release.tag_name }}"
            bun run format:write
            bun run codex:validate
          sync-token: ${{ secrets.TANAAB_COAXIUM_INJECTOR }}
